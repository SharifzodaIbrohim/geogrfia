    show($('resultView'), false);
    show($('examView'), false);
    show($('listView'), true);
    loadList();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', boot);
  } else {
    boot();
  }
})();
