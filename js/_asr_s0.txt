/* Student reg + camera + CSV + folder + Даъватнома */
(function () {
  "use strict";
  var API = (typeof window !== "undefined" && window.API_BASE) ? window.API_BASE : "";
  function esc(s) {
    return String(s == null ? "" : s)
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;");
  }
  function formatTgDate(v) {
    if (!v) return "—";
    try {
      var d = (v instanceof Date) ? v : new Date(v);
      if (isNaN(d.getTime())) return String(v);
      var months = ["январи", "феврали", "марти", "апрели", "майи", "июни", "июли", "августи", "сентябри", "октябри", "ноябри", "декабри"];
      return d.getDate() + " " + months[d.getMonth()] + " соли " + d.getFullYear();
    } catch (e) { return String(v); }
  }
  var _dirMemory = null;
  var _dirHandleKey = "geografia_student_info_dir";
  var _regLock = false;
  var _dirMemory