# Geografia — production / Ubuntu (Python 3.10+)
# pip install -r requirements.txt

flask>=3.0.0,<4
gunicorn>=21.0.0,<27
sqlalchemy>=2.0.0,<3
psycopg2-binary>=2.9.9,<3
google-auth>=2.29.0,<3
requests>=2.31.0,<3
PyJWT>=2.8.0,<3

# Load .env on Ubuntu/local (Render sets env in dashboard)
python-dotenv>=1.0.0,<2
